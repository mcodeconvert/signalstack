# Local dev — copy to .env (gitignored)
DATABASE_URL=postgres://signalstack:dev@localhost:5433/signalstack
ORIGIN=http://localhost:5173
LOG_LEVEL=info

# Email (optional — alerts/digest)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=signalstack@parallelship.com
MAIL_TO=you@parallelship.com
